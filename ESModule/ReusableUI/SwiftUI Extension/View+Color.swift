import SwiftUI

public extension View {
    func foregroundStyle(esColor: Color) -> some View {
        foregroundStyle(esColor)
    }
    
    func backgroundStyle(esColor: Color) -> some View {
        backgroundStyle(esColor)
    }
}
